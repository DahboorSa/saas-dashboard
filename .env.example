# App
# Local dev points at the NestJS backend on localhost.
# In production this is set as a build-time env var on the host
# (e.g. https://nestjs-saas-starter-o50q.onrender.com) — see render.yaml.
VITE_NEST_API_URL=http://localhost:3000